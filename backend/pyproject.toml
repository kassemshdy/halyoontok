[project]
name = "halyoontok"
version = "0.1.0"
description = "Child-safe Arabic-first short-video platform"
requires-python = ">=3.12"

[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.pytest.ini_options]
testpaths = ["tests"]
